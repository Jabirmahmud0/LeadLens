import { LegalPage } from '@/components/LegalPage';
export const metadata = { title: 'Security | LeadLens' };
export default function SecurityTrustPage() { return <LegalPage title="Security and AI Disclosure"><section><h2>Security controls</h2><p>Passwords use Argon2id. Session and recovery tokens are stored as hashes. Organization-owned records are authorization-scoped, and outbound analysis requests enforce SSRF controls.</p></section><section><h2>Public website analysis</h2><p>LeadLens fetches bounded public HTTP/HTTPS content, respects obvious robots restrictions, does not submit forms, and does not bypass authentication.</p></section><section><h2>Responsible reporting</h2><p>Reports distinguish evidence from inference and show sources and limitations where available. Users should independently verify material claims.</p></section></LegalPage>; }
